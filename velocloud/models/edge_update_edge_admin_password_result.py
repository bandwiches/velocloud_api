from dataclasses import dataclass


@dataclass
class EdgeUpdateEdgeAdminPasswordResult:
    """Edge Update Edge Admin Password Result

    Properties:
        id (str): The ID of the action queued
    """
    id = str
