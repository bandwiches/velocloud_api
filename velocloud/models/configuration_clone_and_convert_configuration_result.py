from dataclasses import dataclass


@dataclass
class ConfigurationCloneAndConvertConfigurationResult:
    """Configuration Clone and Convert Configuration Result

    Properties:
        id (int): The ID of the newly cloned configuration
    """
    id: int
