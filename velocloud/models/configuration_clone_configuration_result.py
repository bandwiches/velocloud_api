from dataclasses import dataclass


@dataclass
class ConfigurationCloneConfigurationResult:
    """Configuration Clone Configuration Result

    Properties:
        id (int): The ID of the newly cloned configuration
    """
    id: int
